pandas>=2.2          # engine="calamine" support for fast .xlsx reads
openpyxl>=3.1        # .xlsx writer + calamine fallback reader
python-calamine>=0.2 # fast Rust-based .xlsx reader (primary ingest engine)
rapidfuzz>=3.0
streamlit>=1.30
pyyaml>=6.0
flask>=2.3
flask-cors>=4.0
# xlrd is only needed to read legacy .xls files. Newer xlrd dropped .xls support,
# so it is pinned. Install separately if you must read .xls:  pip install "xlrd==1.2.0"
# sqlite3 is part of the Python standard library — nothing to install.

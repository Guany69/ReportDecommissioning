# Development & test dependencies. Install with:
#   pip install -r requirements-dev.txt
# Keeps testing/linting tools out of the production requirements.txt.
-r requirements.txt

pytest>=8,<10
pytest-cov>=5,<8
httpx>=0.27,<1
numpy>=1.26,<3
ruff>=0.9,<1

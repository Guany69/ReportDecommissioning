# Production dependencies for the Report Decommissioning API (app.main:app).
#
# Pinned with lower+upper bounds so a clean clone, the working tree, and the
# deployment package resolve to the same set. Streamlit is intentionally NOT a
# production dependency; legacy_streamlit_app.py is kept only for regression
# comparison and needs streamlit installed separately if you run it.
#
# The OneDrive, queue, and database integrations add their own dependencies
# (azure-identity, azure-servicebus, azure-storage-blob, pyodbc, sqlalchemy,
# httpx) in their own implementation phases — do not add them here yet.

# --- Engine -----------------------------------------------------------------
pandas>=2.2,<3
openpyxl>=3.1,<4
python-calamine>=0.2,<0.5
rapidfuzz>=3.0,<4
pyyaml>=6.0,<7
xlrd>=2.0.1,<3

# --- Web API service (app.main:app) -----------------------------------------
fastapi>=0.115,<1
gunicorn>=23,<24
uvicorn[standard]>=0.34,<1
uvicorn-worker>=0.4,<1
pydantic-settings>=2.7,<3
